# 回测系统使用指南

## ✅ 已完成功能

### 后端 (Go)
- ✅ 数据库schema (3张表: backtest_runs, backtest_trades, backtest_equity_snapshots)
- ✅ 6个API端点 (`/api/backtest/*`)
- ✅ 完整的执行引擎:
  - TimeSimulator: 时间流模拟
  - HistoricalDataProvider: 历史K线数据加载
  - PositionManager: 持仓管理
  - OrderSimulator: 订单执行模拟
  - BacktestEngine: 完整回测编排

### 前端 (React + TypeScript)
- ✅ BacktestPage: 回测列表和创建页面
- ✅ BacktestDetailPage: 回测结果详情页面
- ✅ 导航菜单集成 (顶部HeaderBar)
- ✅ API客户端方法

## 🚀 如何使用

### 1. 启动系统

```bash
./start.sh
```

### 2. 访问回测页面

登录系统后，有两种方式访问回测功能：

**方式1: 通过导航菜单**
- 点击顶部导航栏的 **"回测"** 按钮

**方式2: 直接访问URL**
- 浏览器访问: `http://localhost:5173/backtest`

### 3. 创建回测

1. 点击 **"新建回测"** 按钮
2. 填写回测配置:
   - **选择交易员**: 从已创建的交易员列表中选择
   - **开始时间**: 回测起始日期和时间
   - **结束时间**: 回测结束日期和时间
   - **初始资金**: 默认10000 USDT (可修改)
   - **使用交易员配置**: 勾选后将使用该交易员的指标配置和提示词

3. 点击 **"开始回测"** 按钮

### 4. 查看回测进度

- 回测会异步执行，在列表中可以看到:
  - ⏳ **等待中** (pending): 回测已创建，等待开始
  - 🔄 **运行中** (running): 正在执行，显示进度百分比
  - ✅ **已完成** (completed): 回测完成，可查看结果
  - ❌ **失败** (failed): 回测失败

### 5. 查看回测结果

点击已完成回测右侧的 **👁️ 查看详情** 按钮，可以看到:

**核心指标**:
- 📈 **总收益**: 总收益率和绝对金额
- 📉 **最大回撤**: 最大回撤百分比
- 📊 **夏普率**: 风险调整后收益
- 🎯 **胜率**: 盈利交易占比

**净值曲线**:
- 时间序列图表显示账户净值变化

**交易记录**:
- 详细的交易历史表格
- 包含: 币种、方向、入场价、出场价、数量、杠杆、盈亏、手续费、时间等信息

### 6. 删除回测

点击回测列表右侧的 **🗑️ 删除** 按钮

## ⚙️ 技术说明

### API端点

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/backtest` | 创建回测 |
| GET | `/api/backtests` | 获取回测列表 |
| GET | `/api/backtest/:id` | 获取回测详情 |
| GET | `/api/backtest/:id/equity-history` | 获取净值历史 |
| GET | `/api/backtest/:id/trades` | 获取交易记录 |
| DELETE | `/api/backtest/:id` | 删除回测 |

### 数据库表

1. **backtest_runs**: 回测运行记录
2. **backtest_trades**: 回测交易明细
3. **backtest_equity_snapshots**: 净值快照

### 执行流程

```
用户创建回测
    ↓
后端验证配置
    ↓
创建数据库记录 (status=pending)
    ↓
异步启动回测引擎
    ↓
加载历史数据 (分批下载K线)
    ↓
时间循环 (每个时间点):
    - 获取市场数据
    - 调用AI决策
    - 执行订单
    - 更新持仓
    - 记录交易
    - 计算指标
    - 更新进度
    ↓
计算最终指标
    ↓
保存结果 (status=completed)
```

## ⚠️ 注意事项

### 时间范围建议

- **测试**: 1-3天 (快速验证)
- **短期**: 1周-1个月 (常规回测)
- **长期**: 1-3个月 (充分验证)
- ⚠️ 不建议超过6个月 (数据量大，耗时长)

### 数据获取

- 系统会自动从交易所API下载历史K线数据
- 下载速度受API限速影响 (~1200请求/分钟)
- 首次回测某时间段会较慢 (需下载数据)

### 性能优化

- 并发运行多个回测时，请注意API限速
- 建议单个回测完成后再启动下一个
- 大时间范围回测可能需要几分钟到十几分钟

## 🐛 故障排查

### 回测一直处于"等待中"状态

- 检查后端日志: 可能是MCP client初始化失败
- 确认交易员配置正确 (AI模型、API密钥等)

### 回测失败

常见原因:
1. **时间范围无效**: 开始时间晚于结束时间
2. **数据获取失败**: 交易所API不可用
3. **AI调用失败**: API密钥无效或余额不足
4. **配置缺失**: 交易员配置不完整

解决方法:
- 查看后端日志获取详细错误信息
- 确认交易员配置正确
- 检查网络连接

### 前端无法访问回测页面

1. 确认前端已重新编译: `cd web && npm run build`
2. 刷新浏览器缓存: Ctrl+Shift+R (Windows) 或 Cmd+Shift+R (Mac)
3. 检查浏览器控制台错误

### 后端API 404错误

1. 确认后端已重新编译: `go build -o nofx main.go`
2. 重启服务: `./start.sh`
3. 测试API: `curl http://localhost:8080/api/health`

## 📊 示例配置

### 快速测试配置

```
交易员: 选择任意已配置的交易员
开始时间: 2025-01-10 00:00
结束时间: 2025-01-12 00:00
初始资金: 10000 USDT
使用交易员配置: ✅ 勾选
```

### 充分验证配置

```
交易员: 你的主力交易员
开始时间: 2024-12-01 00:00
结束时间: 2025-01-01 00:00
初始资金: 100000 USDT
使用交易员配置: ✅ 勾选
```

## 🔄 更新日志

### v1.0.0 (2025-01-16)
- ✅ 完整的回测系统实现
- ✅ 前端UI和导航集成
- ✅ 异步执行和进度追踪
- ✅ 完整的结果分析和可视化

## 📝 TODO

未来可能添加的功能:
- [ ] 批量回测 (多个交易员对比)
- [ ] 参数优化 (网格搜索最优参数)
- [ ] 更多图表 (收益分布、月度收益等)
- [ ] 导出回测报告 (PDF/Excel)
- [ ] 回测模板 (保存常用配置)
